---
import LoginView from '../views/LoginView.astro';

/** Thin per-locale entry point. The page body lives in the view so the two
 *  locales cannot drift apart; this file exists only to name the route and the
 *  language it is in. */
---

<LoginView locale="es" />
